# Restore Test Runner form

The web UI now restores the saved image digest/reference, stand, and scenario from the current
operation after a page reload.

Verification:

- focused Test Runner API/UI tests passed: 3 tests;
- the full non-hardware quality gate passed after staging the intended changes.
