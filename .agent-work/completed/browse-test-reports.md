# Browse Test Runner reports

Added a read-only retained-artifact index with allowlisted downloads and a configurable link to the
Allure Storage repository tree. The browser receives only the public Storage URL, never the report
access token.

Verification:

- focused artifact, API/UI, and settings tests passed: 11 tests;
- the complete Test Runner suite passed: 28 tests;
- standalone and virtual-stand Compose configurations parsed successfully;
- the full non-hardware quality gate passed.
