# Increase Test Runner header gap

The vertical gap between the title/action row and status/report row is now explicitly doubled from
`0.5rem` to `1rem`.

Verification:

- focused Test Runner UI/API tests passed: 4 tests;
- the full non-hardware quality gate passed.
