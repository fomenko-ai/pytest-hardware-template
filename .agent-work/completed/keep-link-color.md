# Keep Test Runner link color

All Test Runner links now retain the system link color after being visited on both the main and
artifact-history pages.

Verification:

- focused Test Runner UI/API tests passed: 4 tests;
- the full non-hardware quality gate passed.
